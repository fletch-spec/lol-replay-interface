# Brief Log

Append-only. One line per state change.

Format: `YYYY-MM-DD | brief-NNN | old-state -> new-state | note`

---

2026-08-04 | brief-001 | - -> ready | queue created
2026-08-04 | brief-001 | ready -> complete | proxy + panel shell built, verified live against replay API, moved to archive
2026-08-04 | brief-002 | - -> ready | queue created
2026-08-04 | brief-002 | ready -> complete | transport control (scrub/pause/speed/keyboard) built, verified live against replay API, moved to archive
2026-08-04 | brief-003 | - -> ready | queue created
2026-08-04 | brief-003 | ready -> complete | PARTIAL: keystroke bridge is a confirmed dead end (PostMessage/SendInput keyboard/SendInput mouse all blocked, likely Vanguard). Pivoted to POST /replay/render {selectionName, cameraAttached} per fletch-spec/lol-path-mapper - selects champion reliably but does NOT deliver a working follow-cam (tried top/focus/path modes; tps crashed the replay once). Shipped brief's own documented fallback: portrait grid, live KDA/CS, click-to-select with real-state lock indicator, no follow-cam. Brief 006 must not assume cameraAttached gives camera follow.
2026-08-04 | brief-004 | - -> ready | queue created
2026-08-04 | brief-004 | ready -> complete | Event timeline built: harvest-on-connect (seek to end, poll for GameEnd, seek back) via brief 002's existing seek path, cached in localStorage by gameMode+length. Markers in-track, clustered, colour-coded; unrecognised event names logged not dropped (caught InhibRespawned live). No API surprises this brief.
2026-08-04 | brief-005 | - -> ready | queue created
2026-08-04 | brief-006 | - -> ready | queue created
2026-08-05 | brief-005 | ready -> in-progress | cue points + A/B loop. Pre-check: /replay/game exposes only processID (per-launch, not stable across sessions) and no path in the whole spec carries a match ID or replay filename, so the brief's primary game-ID suggestion is unusable - falling back to its own documented gameMode+length composite, shared with brief 004's event cache.
2026-08-05 | brief-005 | in-progress -> complete | Cue points + A/B loop built and verified live. One-key M/N/B/[/]/L/Esc; cues seek to t-2s and hold paused via a new holdPaused flag inside the EXISTING doSeek (no second seek path); loop runs off the same 10Hz playback push, inert while paused so pausing mid-loop doesn't cancel it. Cue nav steps an index cursor, not a time search - searching by time re-finds the same cue forever because the lead-in lands you before it. Persistence keyed by gameMode+length via a shared replayIdentity(); survived a full reload and a mid-session replay swap. Export is MM:SS - note, timestamp alone when unannotated, shown in a selected textarea as well as copied. Not done: no UI for per-cue lead-in (stored and honoured, just not editable).
2026-08-05 | brief-006 | ready -> in-progress | HUD toggles + camera presets. Starting with the brief's own Hardest Part (partial POST test) before building anything.
2026-08-05 | brief-006 | in-progress -> complete | Hardest Part was a non-issue: partial POST changed exactly 1 of 66 fields, and the client documents "All values are optional". Real trap is the inverse - unknown field names return HTTP 200 and are silently ignored, so toggles verify by read-back, never by status code. interfaceAll tested and rejected for cinematic (takes the replay control bar); composed from 16 individual fields with interfaceReplay excluded. Cinematic restore verified exact from a mixed starting state, not defaults. Presets keyed by mapName, verified round-trip. tps never sent. Not done: depth of field / fog.
2026-08-05 | brief-007 | - -> ready | clickable controls + layout review. Written after 005 shipped keyboard-only cue/loop actions: 8 of 12 panel actions are hotkey-only, and hotkeys don't fire while the replay client holds focus, which is the whole narration scenario. Also folds in the floating-list-covers-the-timeline problem and the open layout bugs. Independent of 006; recommend running it after.
2026-08-06 | brief-007 | ready -> in-progress | clickable controls + layout review, last in the queue. Starting with the command table (the brief's second risk) so buttons and hotkeys cannot diverge, then the floating-list-covers-the-timeline fix.
2026-08-06 | brief-007 | in-progress -> complete | Command table built first so buttons and hotkeys share one implementation - proved live by arming the loop with the button and disarming it with L, with the button's visual state following. All 9 keyboard-only actions now have buttons carrying their own hotkey label, grouped into transport and cue cards rather than one toolbar. Floating event/cue dropdowns replaced by an in-flow 330px tabbed rail, so nothing covers the scrub track any more. Folded in all three known layout bugs: speed-button double highlight, no seek-in-flight feedback, empty objective lane. Pause button was the one action still bypassing the table; routed through it. Panel 1400x887 in 1919x905, no overflow. Not done: legend kept - marker colours are still unexplained elsewhere.
2026-08-06 | brief-008 | - -> ready | depth of field + fog, closes issue #6. Deferred from 006 because the toggle grid was full and 007 was about to rewrite the layout; 007 has landed so that objection is gone. Hardest Part is a decision rather than a build: DoF focal bands are camera-relative and the follow-cam distance slider moves the camera 700-14000 units, so either the bands scale with followDistance or DoF is preset-only. Pick before building UI. Presets must carry the settings and must still load if saved before this brief.
2026-08-06 | brief-008 | ready -> ready | RENUMBERED to brief-012. This project's numbers are execution order, and depth of field / fog belongs last - it is the largest new surface and wants the camera work settled underneath it first.
2026-08-06 | brief-008 | - -> ready | camera stability, closes issue #3. Testing brief before a building brief: establish what cameraLockX/Y/Z actually lock (the entire documentation is "Lock FPS Camera at x axis") before deciding the guard. Acceptance test is a locked shot surviving ten seconds of mouse movement over the game window. Try cameraLookSpeed:0 first as the cheap experiment.
2026-08-06 | brief-009 | - -> ready | recording-safe event harvest, closes issue #4. Not "undo brief 004's harvest decision" but "keep the complete timeline without the jump". Three options costed in the brief; a fourth worth ten minutes first - whether the harvest can be made invisible rather than avoided, now that 006 can hide the HUD and 008 will have settled the camera.
2026-08-06 | brief-010 | - -> ready | event feed audit, closes issue #7. Read-only investigation across two more replays, at least one known to contain a dragon and a baron. The risk is stopping at the first answer: "this replay also has none" is not "the feed omits them".
2026-08-06 | brief-011 | - -> ready | narration papercuts, closes issues #2, #5 and #8. Gnar portrait 404s, uneditable cue lead-in, silent dead keypresses. Hardest Part is not letting the focus indicator grow into a focus-management system.
2026-08-06 | brief-012 | ready -> cut | Depth of field + fog cut, not deferred. Fails the project's own scope test: it is a look decision set once and never touched during a take, so it is configuration rather than live control. Brief 006 undermined itself asking for it ("distracting over 30 minutes of narration"). For analysis narration DoF and depth fog actively remove the information you are pointing at. The one legitimate use - directing viewer attention - is already served by the follow-cam and distance slider. Traced its origin: 006 listed the fields from a remembered wishlist, "not done" became a KNOWN_ISSUES entry, which became issue #6. A self-referential todo, never a requirement.
2026-08-06 | - | scope review | Camera presets removed. Built under brief 006 when the keystroke bridge had failed and 006's own note said presets were "your only camera control"; follow-cam was then solved outside the queue and nobody re-evaluated. Same failure mode as the depth-of-field cut: a feature surviving on the inertia of a premise that no longer held. The HUD toggle grid moved behind a collapsed Setup disclosure for the same reason - 17 chips of permanent panel space for something configured once, where Cinematic is the control actually pressed during a take. Also retired the UI polish plan page: phases 1-2 shipped directly, 3 landed inside brief 007, 4 was absorbed by 007 and 011, so it read as a live plan while being a finished one.
2026-08-06 | - | session wrap | Post-brief UI pass, all outside the queue. Panel reordered to statusbar -> roster -> transport controls -> timeline -> cue controls, with the three transport sections combined into one card divided by hairlines rather than three stacked cards. Status bar carries state on the left and actions on the right: Cinematic, Setup and the camera distance slider. Removed the 1-5 / Q-T badges from roster portraits - they labelled in-game spectator hotkeys the browser can never send, left over from brief 003's dead keystroke bridge, so they read as panel shortcuts that did nothing. Repo published at github.com/fletch-spec/lol-replay-interface with docs on the wiki and open problems as issues; every actionable issue now maps to one of briefs 008-011.
2026-08-07 | brief-013 | - -> ready | Cue notes as the primary surface, closes issue #11. First of three briefs from the timeline/event overhaul review. The panel's main operation - mark a moment, write what to say about it - has the smallest surface on the panel: a single-line input in a 330px rail, where paragraph notes go invisible the moment focus leaves. Hardest Part is not the widget but the export contract: cuesAsText() emits MM:SS - note as YouTube chapters and a paragraph breaks it, so a note probably needs a short label separate from its body. Collides with brief 011's per-cue lead-in control over the same row - see the review note below.
2026-08-07 | brief-014 | - -> ready | Event marker legibility, from the same review. Markers get ~2.5s of horizontal room each at panel width and any real fight collapses into an unreadable picket fence; the hover card that resolves it is good but undiscoverable. Written to head off the obvious wrong fix: widening markers merges more events per cluster and makes the timeline less informative, so this is a density problem, not a CSS one. Acceptance is explicitly tested at ~130 events, not the 6-event screenshot. After 009 and 010 - 010 decides whether the objective lane exists and 009 decides whether a fresh replay has a full timeline to lay out at all.
2026-08-07 | brief-015 | - -> ready | Roster placement and spacing pass, from the same review. Roster is the largest block on the panel for glanceable info and sits above the instrument; proposal is two columns beside the event/cue rail. Hardest Part is that the horizontal budget is already spoken for - the panel is 1400px wide for pixels-per-minute on the scrub track, which is exactly what 014 is about to spend, so a roster column beside the rail trades one review complaint for another. Brief requires stating track width before and after rather than shipping a narrower timeline by accident. Third layout pass on this panel, so scoped to what the review names and explicitly barred from the status bar, rail, readout and legend, which it called fine.
2026-08-07 | - | review | Timeline/event overhaul review (annotated screenshots, 2026-08-07) triaged into 013-015. Two sequencing conflicts recorded rather than resolved: (1) 011's per-cue lead-in control and 013's cue row redesign own the same row, so whichever runs second inherits the other's design - moving that one item from 011 into 013 would make numbers match execution order again, but 011 is already written and logged, so it is the user's call; (2) the review's screenshots show a 29-minute replay reporting 4 events, all at 29:01-29:11, growing to 6 as playback passed 1:27 - consistent with the harvest populating only end-of-game events rather than the full feed. That is evidence for brief 010's audit, not overhaul scope, but 010 should not assume the harvest is whole. Review also confirmed working and out of scope: status bar, event/cue rail, time readout and legend, camera lock on portrait click, target frame on info click.
2026-08-07 | - | scope review | All 9 open issues assessed against the project's scope test, same treatment as the brief-012 cut. KEEP as filed: #2 (Gnar 404), #3 (camera drift), #4 (harvest yanks playback), #7 (event feed audit) - all four are defects in live control, and #4 is the most severe thing open, since it breaks the single-pass recording the project exists for. REFRAMED: #5 (cue lead-in) moved from brief 011 to brief 013, because both wanted a control in the cue row and 013 redesigns that row - whichever ran second would have inherited the other's layout. #8 (dead keypresses) kept but confirmed as a chip and nothing more; a focus-management system fails the scope test the same way camera presets did. #11 (cue note improvement) becomes brief 013 and gains an explicit anti-scope fence: notes are live prompts written while watching, not a script composed beforehand, so no rich text, tags, reordering, templates or non-localStorage storage. That fence is the main output of this review - "expanded greatly" is exactly the phrasing that turned into depth-of-field and camera presets before. RECOMMEND CLOSING (not actioned, user's call): #9 and #10 are labelled wontfix and are permanent constraint records rather than work - both are already in the wiki and #9 is in the README; leaving them open makes the issue list read as 9 open problems when 7 are actionable. NOT CUT, but flagged: cuesAsText() exports YouTube chapters, which is arguably post-production rather than live control - it survives because the notes are written live and the export only reads them back, but it is the closest thing in the panel to a scope violation and should not be extended.
2026-08-07 | brief-008 | ready -> ready | REWRITTEN verbose for Sonnet execution. Was a testing brief that said "find out what cameraLockX/Y/Z do"; now an ordered cheapest-first experiment protocol - cameraLookSpeed:0, then the three lock flags one at a time, then drift-detection as an explicit fallback that is not built if the first two work. Added exact curl commands through the panel's own proxy on :3000 (avoids the self-signed cert on :2999), a code-anchor table, and the acceptance test as numbered steps. Traps section names the four ways this fails silently: 200-on-unknown-field, cameraMode resetting fieldOfView, selectionOffset detaching the follow without selectionName, and a 1Hz re-assert loop fighting deliberate manual framing.
2026-08-07 | brief-009 | ready -> ready | REWRITTEN verbose, and the deferred decision made. Was "three options, 2 with a good escape hatch is probably the answer". Now ships option 1, explicit scan-on-request, with the other three rejected in writing: deferred harvest still moves the playhead unasked and relies on the user not having hit record; progressive-only is useless on a first watch and already happens via mergeEvents anyway; invisible harvest does not survive contact because the jump is in the footage, not the HUD. Keeps the ten-minute allgamedata check as step 1 because it can collapse the whole brief. New action goes in the COMMANDS table per brief 007 rather than a bare listener. Adds the interrupt case brief 004 never handled - the seek-back only happens on the success path, so an interrupted harvest strands playback at the end of the game.
2026-08-07 | brief-010 | ready -> ready | REWRITTEN verbose, and given a new first step. The 2026-08-07 UI review's screenshots show a 29:20 replay reporting 4 events, all at 29:01-29:11, growing to 6 as playback passed 1:27 - which is consistent with the harvest populating only end-of-game events. If that is right, this brief's entire premise (99 kills and no dragons) describes a partial harvest and the objective question was never tested. Brief now checks harvest completeness before auditing anything and stops if it is incomplete. Kept the original brief's real insight - "this replay also has no dragons" is the absence of a finding, not a finding - and made it the stated trap.
2026-08-07 | brief-011 | ready -> ready | REWRITTEN verbose and narrowed from three items to two. Lead-in editing (#5) moved to brief 013. Gnar fix pinned to server.js servePortrait() rather than the client, because the roster rebuilds every img at 1Hz so any client-side retry suppression is wiped by the next render. Added a hard rule against inventing alias strings from memory - a wrong alias shows the wrong champion's face silently, which is worse than a 404 - so the brief instructs collecting real championName values by logging them from pollRoster first. Focus chip explicitly capped at one chip with fixed wording ("click panel for keys", about the panel not the game).
2026-08-07 | brief-013 | ready -> ready | REWRITTEN verbose, and both open design questions resolved. Note shape is label + optional body: note keeps its exact current meaning so the export, the pin label and every already-saved cue are untouched, and there is no migration step at all - if one is being written, the shape is wrong. Editor is a full-width section inside the transport card between the timeline row and the cue controls, which inherits the existing hairline-separated section pattern, does not overlay the scrub track, and does not spend the horizontal budget briefs 014 and 015 are contending for. Selecting a cue opens the editor but never focuses it, which is what preserves brief 005's M-stays-instant rule. Traps name the two things that will actually break it: re-rendering the editor from its own input event destroys the caret, and placeCue()'s sort plus deleteCue()'s splice both invalidate activeCueIndex mid-edit.
2026-08-07 | brief-014 | ready -> ready | REWRITTEN verbose, decision made: counted clusters plus a gutter-wide hover target, reusing the existing SNAP_PX nearest-cluster loop rather than adding a second one. Density heat strip rejected as redundant once cluster width scales with count; detail band rejected as the right follow-up but wrong brief, since it claims vertical space exactly when 013 has added a section and 015 has to balance the panel. Lowering CLUSTER_PX rejected - it trades an honest cluster for a dishonest pile. Step 1 is now "get ~130 events on screen before writing code", because every decision here is invisible at six events and will look fine while failing in use. Records the ambiguity in the source feedback ("hard to see of the start") rather than silently picking a reading.
2026-08-07 | brief-015 | ready -> ready | REWRITTEN verbose, and the review's own suggestion costed and rejected. Roster does not move next to the rail: the rail is inside the timeline row, so every pixel it takes comes off the scrub track, which is at 1400px for pixels-per-minute and which brief 014 has just spent. Trading marker legibility for roster placement swaps one review complaint directly for another, and the complaint was vertical space anyway. Primary approach is one row of ten cards with a stated legibility gate at 1400px (~128px per card against a 44px portrait) and a two-compact-rows fallback if names do not fit - either outcome passes, a 9px champion name does not. Spacing becomes four custom properties rather than per-element nudges. Transport row gets grouping via the existing .control-sep, not different gap values, because its oddness is nine ungrouped buttons rather than a wrong number. Concrete defects folded in: .roster-info declared twice, .roster-list space-between defeating its own gap, the 13px outlier in .transport > *.
2026-08-07 | - | process | briefs/template.md rewritten to the verbose shape (Decision / Where The Code Is / Implementation Steps with Done-when / Verification / Traps / Escalate). briefs/PASSOFF.md rewritten for Sonnet sessions - it was still saying 005 and 006 remained. New passoff front-loads five facts as the first thing read (200-on-unknown-field is now first, ahead of the tps crash, because it is the most common way to "finish" a brief that does not work), states the queue as 008 -> 009 -> 010 -> 011 -> 013 -> 014 -> 015, and tells the session the Decision sections are settled and to stop rather than substitute its own judgement. Execution order now matches numeric order again as a side effect of moving #5 out of 011, so no renumbering was needed.
2026-08-07 | - | process | PASSOFF.md replaced with a short staged session prompt - 90 lines of how-to-run-a-brief down to four stage gates (Orient / Build / Verify / Close) with stops after 1, 3 and 4. The long version existed because briefs deferred their design decisions to whoever picked them up; now that each brief carries its own Decision, anchors, steps, traps and acceptance test, the prompt only has to set the frame, the five API facts and the gates. Five facts kept inside the pasteable block rather than linked, since a session that only receives the paste has to see them. Stage 3 gate is deliberately "stop and wait for me to test it myself" - the previous prompt asked the session to self-report done, which is the step that let brief 004 ship broken.
2026-08-07 | brief-008 | ready -> complete | Camera stability, closes issue #3. Reproduced live (user's own hand on the mouse - this session cannot drive one): cameraRotation swung from the locked {0,56,0} baseline to ~15-20 degrees sustained, spiking past 300 on fast swings, cameraMode staying fps throughout. Experiment 1 (cameraLookSpeed:0) suppressed it, confirmed directly by the user re-testing. Shipped in both lockCamera() and applyFraming() - the slider re-frame re-asserts the whole camera body, so a fix in only one place would have reopened the drift the next time someone dragged the slider mid-take. Experiments 2 and 3 not built, per the brief's own stop-when-an-earlier-one-works instruction - the three cameraLockX/Y/Z flags remain formally untested. Verification step 7 ("click to unlock") doesn't correspond to anything lockCamera() actually does - there is no unlock toggle in the codebase, re-clicking a locked portrait just re-locks. Flagged rather than built, since a free-fly control is new scope, not a guard.
2026-08-07 | brief-009 | ready -> complete | Recording-safe event harvest. harvestEvents() split into loadCachedEvents() (cache-only, automatic, never seeks) and scanReplay() (seek-to-end-and-back, only from the new hotkey-less scanReplay command). Three timeline states verified live (not scanned / scanning… / scanned), double-scan guarded, interrupted scan confirmed to still restore the playhead and skip the cache write (tested by instrumenting requestSeek directly, not by trusting state after the fact). commandButton() needed a guard for hotkey-less commands, which would otherwise render the word "undefined". Found and reported rather than fixed: the inherited seek-to-end harvest mechanism (unchanged since brief 004) does not reliably capture the whole game - a scan against the live 1698s replay cached only 5-10 events against at least 22 discoverable by seeking around directly, and the count went flat immediately rather than growing over a 10s wait, ruling out "just needs more time". This is brief 010's own stop condition, reached one brief early - see brief 010 for how it was handled. All "watch the game window" verification steps were done by polling /api/replay/playback instead, since this session has no access to the actual client window, only the panel and the API.
2026-08-07 | brief-010 | ready -> complete | Event feed audit. Stopped at step 1 per the brief's own instruction: the harvest is confirmed incomplete. A fresh Scan through the real panel UI cached 15 events; an independent query of /liveclientdata/eventdata at the same position seconds later found 24, including 19 ChampionKill against the scan's ~10-11. Same replay, same position, two different counts by code path alone - ruled out as a timing issue in brief 009's own testing already. Did not proceed to picking dragon/baron replays or touching EVENT_CATEGORY/the legend, per the brief's explicit stop condition - auditing objectives against a harvest that drops a third of known ChampionKills would be the "confident wrong answer" the brief exists to prevent. Also flagged, independent of the harvest bug: picking replays "known to contain a dragon and a baron" needs the user's knowledge or a look at the actual game, neither available this session. Dragon/baron question left genuinely open, not answered either way. No code changes, per this brief's read-only constraint.
2026-08-07 | brief-011 | ready -> complete | Narration papercuts. Gnar fix shipped as a server.js alias table (CHAMPION_NAME_ALIASES) consulted in servePortrait() before the championKeyMap lookup; verified live (roster's real <img> src, not the fallback) by seeking the loaded replay to a point where Gnar was transformed. Unresolved names log once, verified with a bogus name. Did not guess the rest of the champion list per the brief's own instruction - swept the one available replay's full timeline against playerlist instead of eyeballing it: Mega Gnar was the only transformed name observed, Kayn was present all game and never transformed, everyone else on the brief's list wasn't in the roster at all. Left out rather than guessed, and said so in the alias table's own comment. Focus chip added (document.hasFocus() + focus/blur, matches the existing .chip.loop show/hide pattern, no polling). Verified the chip's logic both directions by overriding document.hasFocus() directly, since this session cannot generate a real OS focus change onto a second window - flagged that gap explicitly rather than claiming a click-through test that didn't happen.
2026-08-07 | brief-013 | ready -> complete | Cue notes as the primary surface. Full-width editor section added to .transport between the timeline row and cue controls, carrying label/body/lead/delete; note+body+lead persist, body never appears in the export or the pin hover card, verified live. Selection tracked by object identity (activeCue) rather than index per the brief's own Traps warning - proved live that the editor survives a placeCue() sort that shifts the selected cue's index. renderCueEditor() only ever runs from an actual selection change, never from the editor's own input handlers, which was tested directly by checking textarea cursor position held across an input event. Found and fixed a real pre-existing bug while building this: Escape-then-blur() in a field lets the same keystroke bubble to the document-level handler after document.activeElement has already changed, silently firing loopClear - reproduced on the old .cue-note field before touching it, fixed there and in both new fields with stopPropagation(). Auto-switch-to-Cues-tab implemented as a one-time page-lifetime flag, not per-identity. Measured, not assumed: scrub track held at 982px and panel measured 1400x856 at 1920x1080 with the editor open and a 30-line stress-test note, so brief 015 inherits no width/height debt from this one.
2026-08-07 | brief-014 | ready -> complete | Event marker legibility. Counted clusters (8 + min(count,6)*3 px, capped ~26px, grows from the left edge rather than centring per the brief's own trap about not visually claiming time before the first event) plus a gutter-wide hover magnet sharing one nearestCluster(px, maxDistance, lane) helper factored out of targetFromEvent(). Y-to-lane mapping is two hardcoded thresholds tied to the existing .marker.laneN CSS positions. Verified live: swept 418 samples across a 28-cluster lane with zero dead spots; confirmed the lane-collapse trap specifically by hovering the slot an empty objective lane vacates and getting tower events, not nothing. Substituted the available replay's own accumulated 61-event/34-cluster pool for the brief's named 130-event test replay, since this session can't browse or load replay files - noted rather than silently substituted. Also flagged: the Browser pane used for testing never actually composites in this environment (a screenshot times out), so all verification here is DOM-measurement-based, not eyes-on-pixels; a resize_window call was needed to get the track past a ~2px "backgrounded tab" layout.
2026-08-07 | brief-015 | ready -> complete | Roster density and spacing pass. Built the single-row-of-ten primary approach first and measured it honestly: at 1400px the champion-name element had 37px to work with, not enough even for "Renekton" - failed the legibility gate, so shipped the documented two-row fallback instead (32px portrait, tighter padding, hint moved to a title attribute). Real before/after at 1400px against a live connected replay (the pre-session committed file was temporarily served from public/ for a true "before" number, then deleted): scrub track 934px unchanged, roster 196px -> 176px, panel 694px -> 602px. Collapsed the duplicate .roster-info, fixed .roster-list's space-between fighting its own gap, added the four --space-N tokens and applied them across .panel/.card/.transport>*/.roster*/.cue-controls/.transport-controls (13px is gone), gave the roster a card border, grouped .transport-controls into seek|pause|speed|events with .control-sep (also fixed seekFwd sitting apart from seekBack, a pre-existing split). Found and fixed a real bug from brief 013 in the process: .cue-editor's own `display: flex` rule beat the browser's default [hidden] { display: none } at equal specificity, so the editor was rendering (~160px) even when .hidden correctly read true in JS - every check in 013's own outcome tested the property, never the rendered box. This brief's own measure-first step is what caught it, since the panel height numbers didn't add up otherwise. Addended into brief-013.md's archived outcome as well as recorded here.
2026-08-07 | - | triage | Second triage pass, all 8 open issues dispositioned against the scope test. Six briefs written (016-021), queue is 016 -> 017 -> 018 -> 019 -> 020 -> 021. Nothing cut this pass - unlike the 2026-08-06 review, every open issue is either a defect in live control or a defect in reading the panel while narrating, so the scope test cleared all of them. Three findings the issues themselves did not carry. (1) The most severe open defect has no issue number: the event harvest loses roughly a third of a game, found inside brief 009's testing and confirmed by brief 010, and it silently degrades the timeline, the event list, the next-event readout and brief 014's cluster counts. It became brief 016 and it goes first because 017's dedupe, 019's objective lane and issue #7 all read the data it fixes. (2) Issue #12 asks for cue tags, which brief 013's anti-scope fence explicitly barred and which the 2026-08-06 scope review recorded as its main output - flagged in brief 020 rather than absorbed, because reversing that fence is a deliberate call and not a sub-bullet of an export brief. (3) Issue #16 has an empty body, so brief 021 is written as reproduce-first and is allowed to end in a wontfix constraint record like #9 and #10 rather than a fix. Unchanged from the last pass: #9 and #10 are constraint records rather than work and are still recommended for closing, and #7 stays open and blocked rather than becoming a brief, since the question it asks cannot be answered against a harvest that drops events.
2026-08-07 | brief-016 | - -> ready | The event harvest loses events, first in the queue and the reason the rest are ordered as they are. No issue number - this is the defect brief 009 found while closing #4 and brief 010 stopped on, and the brief instructs filing an issue when it is picked up. Resolves the open decision the last session left for the user (reopen 004 / extend 009 / own brief) in favour of its own brief: 004 and 009 are archived and the log treats archived briefs as closed history. Hardest Part was resisting the fix. Two sessions have already measured this from the outside as a count, so the brief spends its first two steps instrumenting and forbids skipping to a change. Triage did find a concrete anchor worth writing down: the panel never fetches events itself - server.js pollRoster() requests eventdata inside a four-way Promise.all whose catch is empty by design, so one failed request drops the whole tick including cumulative event state, and scanReplay() only gets about three 1Hz ticks before its GameEnd break fires. That fits the measurement (15 cached against 24 by direct query, same position) and explains why the two code paths disagreed, but the brief states it as the leading hypothesis to test rather than the answer, and says the finding is worth more than the fix if it turns out wrong. Second candidate recorded: the GameEnd early break, which brief 010 already showed is not a completion signal.
2026-08-07 | brief-017 | - -> ready | Event labels leak engine IDs and some events land twice, closes #13. Three defects from one pasted event list. Raw structure and minion IDs reach the screen because championName() returns its input unchanged when the roster does not resolve it - the comment at 2110 already documents this as known and it renders anyway. The 200ms dedupe bucket was sized against millisecond jitter and the observed jitter is a full second, so the same turret kill appears at 2:04 and 2:05. And derived events (FirstBrick, FirstBlood, Multikill, Ace) render as peers of the events they describe, so First Tower restates the turret line directly above it. Hardest Part is the second one: widening the bucket to 2s is safe for ChampionKill because respawn timers make a false merge impossible, and unsafe for Multikill, which fires repeatedly for the same killer with no victim to separate the copies - so KillStreak goes into the fingerprint tuple in the same change, ordered first, or widening the bucket eats multikills on the way past. Rejected merging FirstBrick into its TurretKilled: the row is a seek target and one row cannot seek to two times. Carries brief 011's rule forward - collect real killer names by logging them before writing any matcher, never from memory.
2026-08-07 | brief-018 | - -> ready | The hover card is clipped by the card it lives in, closes #15. Mechanism found by grep during triage rather than left as a design question: .hover-card grows upward from the top of .scrub-area and .transport.card sets overflow hidden, so everything above the transport card's edge is cut. Headroom is about 50px against a 240px max-height, which is why the screenshot shows most of a card missing. Brief 014 turned this from an edge case into the normal case - counted clusters mean a hover lists eight or twelve rows and the gutter-wide target means cards open on a sweep. The rows lost are the earliest in the cluster, since the list is time-ordered and grows off the top, so you lose the start of the fight you were about to talk about. Decision is position fixed anchored from the element's viewport rect, with the containing-block condition (transform, filter, will-change, contain on any ancestor) checked as step 1 because it decides whether the card can stay in the DOM or has to move to body. Rejected dropping overflow hidden - it is what keeps the hairline sections inside the card's rounded corners - and rejected flipping the card below the track, which breaks the rule briefs 007, 013 and 014 all keep. Notes brief 014's warning that the Browser pane here may not composite, and that a clipping bug is genuinely visual: numbers alone do not close it.
2026-08-07 | brief-019 | - -> ready | Marker colour, marker shape and what counts as an objective, closes #14. Four complaints that are the same complaint four times - the timeline encodes more than a 4x9px marker can carry. Measured the actual collision rather than restating it: --kill is #e05c5c and the Chaos team colour --red is #e0576b, four points apart in the red channel, and team gets 3px of a 9px marker while category is already encoded twice as lane position and hue. Decision is to swap the starved channel for the redundant one - team owns the marker body, category owns the lane and the shape - and delete the cap. Kills get an X per the issue, with brief 015's pattern applied deliberately: a stated legibility gate and a documented diamond fallback, either outcome passes, a smudge that is technically an X does not. Clusters keep brief 014's counted bar untouched, since a cluster is a number and not a shape. Declined the issue's proposal to merge objectives into structures - it costs the lane separation brief 004 built and puts a first tower next to a baron - and fixed the real cause instead, which is a legend naming two abstractions that do not explain their own split. Also found during triage: void grubs have no EVENT_CATEGORY entry at all, so they fall through to unknown and render grey in the structures lane, which is part of what "objective indicators missing" means independent of #7.
2026-08-07 | brief-020 | - -> ready | Cue notes as markdown in their own page, closes #12 except the tags. Brief 013 made the note body the panel's primary surface and there is no way to get it out - cuesAsText() emits MM:SS - note for YouTube chapters and 013 verified the body's exclusion on purpose, so twenty cues means twenty clicks to read back your own notes inside the tool you are about to close. Decision is a second export rather than a changed one, since the chapter shape is a contract with something outside the panel. Raw markdown in a textarea, not rendered - the panel has no build step and rendering means shipping a parser to make text look like text you were going to copy. Hardest Part is not the format: window.open costs keyboard focus, and the panel's hotkeys only work while it has focus, which is exactly what brief 011 shipped a chip for. So the in-page textarea stays as the no-focus-cost path during a take and the reason is written into the button's own comment. Tags are explicitly out of scope with the reasoning stated rather than quietly dropped - see the triage line above.
2026-08-07 | brief-021 | - -> ready | Streamer mode versus camera control, closes #16. The issue has an empty body and the brief says so at the top, so everything below its Problem Statement is reconstruction from the code and step 1 is confirming the symptom exists. Written as an investigation brief in brief 008's shape - cheapest-first, ships code only if the evidence points at the panel - because the three possible outcomes lead to completely different work: a panel bug, a client constraint like #9 and #10 where the deliverable is a record and one chip, or not reproducible at all. This project has already paid for guessing which applies; briefs 003 and 006 built an entire keystroke bridge against outcome 2. Explicitly rejected before starting: building a detect-and-warn indicator first (wrong if the panel is the bug), re-asserting the camera on a timer (brief 008 rejected that for a reason that still holds), and any keystroke fallback (#10). Notes that streamer mode may have no API representation at all, in which case documentation-only is an acceptable result and inventing detection is not.
2026-08-07 | - | process | briefs/TRIAGE.md written - the companion to PASSOFF.md, covering how the queue gets filled rather than how it gets run. Codifies what this pass actually did: read the wontfix issues too (they are the record of what not to retry), open the code for every issue before writing anything, apply the scope test out loud per issue, give every issue one of five dispositions where only one is a brief, and keep numeric order equal to execution order by picking numbers after the order. Carries the rules that came from getting it wrong - a brief that defers its design decisions is not finished (the reason PASSOFF shrank to four stage gates), never invent strings from memory (brief 011), state the gate and let the brief fail it honestly (brief 015), the absence of a finding is not a finding (brief 010), surface fence reversals rather than absorbing them (#12), and severity is not position in the queue since the worst open defect had no issue at all. Hard rule at the end: triage writes no code, not even the one-liner it can see from here, because a half-applied fix makes the brief's line anchors lie. Also added .claude/skills/triage-briefs/ as a thin entry point pointing at TRIAGE.md, so the process is invocable rather than remembered. PASSOFF.md updated - queue line, archive range, commit reference for line-number drift (d0ae049 -> e8e05b9), and a pointer to TRIAGE.md for when the queue runs dry.
2026-08-08 | brief-016 | ready -> complete | The event harvest loses events. Leading hypothesis (Promise.all sharing failure with the empty catch) not confirmed - instrumented pollRoster() through 400+ ticks including a rapid-fire seek stress test built specifically to provoke it, zero drops. Shipped Promise.allSettled anyway, events and roster broadcast independently, on the strength of the code reading rather than a reproduction - said so plainly rather than claiming a catch. The real, reproduced bug was the second candidate: scanReplay()'s GameEnd-early-break races the same 1Hz tick still merging events in the background, caught directly as a cached-vs-live off-by-one within 150ms of harvestDone flipping true (28 vs 29). Replaced with a poll-until-eventsByKey.size-stops-changing condition (3 stable 500ms ticks, ~10s hard ceiling); verified twice, cached === live exactly both times (29 = 29, identical per-EventName breakdown), settling in ~2s each run. Found and handed to brief 017 rather than fixed here: the client's own eventdata is not idempotent across seeks - re-passing a time range re-emits the same event with a new EventID and up to ~1s of EventTime jitter, which is most of what made a raw query look like it disagreed with the panel and is likely the real explanation for briefs 009/010's original 15-vs-24 measurement. No GitHub issue filed - the brief asks for one but that's public-facing and wasn't authorized this session.
2026-08-08 | brief-017 | ready -> complete | Event labels leak engine IDs and some events land twice, closes #13. Step 1 (collect real unresolved names from a live game) couldn't run as written - this replay has zero TurretKilled/InhibKilled/objective events at all (per brief 016's own scan). Built the resolver against the two shapes the issue itself quotes from its own real game instead of inventing anything, plus a logged shape-based catch-all for anything else; verified synthetically (injected fake events, called describeEvent() directly) since no live example existed - said plainly that this is code-path verification, weaker than a live reproduction. The dedupe bucket widen (200ms -> 2s, KillStreak added to the fingerprint first per the brief's own ordering) is a real but incomplete fix: reprocessing brief 016's cached 29-event array dropped it to 27, exactly 2 named duplicate pairs merged, but 3 other known jittered pairs (gaps of 0.91s, 0.25s, 0.18s) still didn't merge because they straddle the fixed bucket's grid boundary - confirmed live in the rendered list, reported rather than silently re-tuned per the brief's own Escalate instruction. The ★-prefixed derived-event redesign (First Blood/multikill/Ace/First Tower) verified live and clean. All three label surfaces confirmed to share one describeEvent()/categoryFor() path. Cues confirmed by code read to be fully decoupled from the fingerprint (raw EventTime only), not live-click-tested given this session's known Browser-pane interaction limits.
2026-08-08 | brief-018 | ready -> complete | The hover card is clipped by the card it lives in, closes #15. Step 1's containing-block check ran live first as instructed and came back empty, confirming position: fixed can stay in place rather than moving to document.body. openHoverCard() now computes position from the anchor's own getBoundingClientRect() and the viewport, not from .scrub-area. The direction split the brief's own Trap warned about was real, not cosmetic: markers grow upward (away from the track) and cue pins grow downward (also away from the track) - a naive "always grow up" would have put a cue pin's card on top of the track itself. Both directions clamp their own max-height to available space rather than flipping, preserving the three-brief-old "nothing covers the track" rule. Verified live with real numbers: busiest cluster (8, later 21) rendered fully with overlapsTrack false every time; leftmost/rightmost markers stayed on screen at 1920px; cue pin card confirmed growing downward; hover grace, row click (seeks + hides), shift+click (cues without moving playback), and resize/scroll (hide, the simpler of the two allowed options) all confirmed live. Screenshot comparison could not run - this session's Browser pane doesn't composite frames, same limitation brief 014 hit - said so rather than skipped silently; relied on the diff touching zero lines of .transport.card's own CSS as weaker but real evidence instead. Removed pinPixelX() and the scrubAreaEl reference as dead code once position moved off .scrub-area.
2026-08-08 | brief-019 | ready -> complete | Marker colour, marker shape and what counts as an objective, closes #14. Steps 1-2 hit the brief's own escalate condition narrowly: this session's only replay has zero neutral objective events of any kind (confirmed by brief 016's own scan and zero unrecognised-EventName logs all session), so EVENT_CATEGORY was left untouched rather than inventing a grub entry - read literally, the brief says stop before restyling the empty lane, not stop the brief, so steps 3-9 shipped. Team now owns marker colour (team-order/team-chaos/team-none from the unchanged teamFor(), replacing the .cap child element, .cap CSS deleted) - verified live, exact colour match on all 5 real clusters, cluster widths exact-matched clusterWidthPx() on all 5 confirming brief 014's bar is untouched. Single-event shapes (diamond for kill/objective, plain rectangle for tower/unknown) verified synthetically via direct buildMarker() calls, since no single-event marker exists in the live data. Shipped the documented diamond fallback for kills rather than the X - the legibility gate needs eyes on pixels and this session's Browser pane still doesn't composite (same as briefs 014, 018), so an unverified glyph would be exactly the "smudge" the gate exists to catch. Legend updated (named categories, "Marker colour = team" replacing "Top cap = team"); .legend-dot/.hc-dot/.event-dot decided to stay categorical, not team-coloured, written down in a comment - found and fixed a real pre-existing bug in passing, .event-dot.tower was using --accent (blue) against every other surface's --struct (grey). Checked the two numeric traps by computation instead of by eye: blue-marker-on-blue-fill composites to a real ~2-3x brightness jump not a collision, and the count digit's contrast ratio (3.87-4.11) is within noise of the old category colours (3.92-5.80) it replaced.
2026-08-08 | brief-020 | ready -> complete | Export cue notes as markdown in their own page, closes #12 except tags. cuesAsMarkdown() built as a pure function beside cuesAsText(), verified directly in console against a 6-cue set covering every named case (label+body, body-only, label-only, neither, and one with script tags/entities/backticks/# ) - output matched the brief's example shape exactly. window.open() was blocked by this session's Browser pane on every attempt, so verification step 10 (the popup-blocked fallback) is what actually got tested live rather than the primary path - screenshotted the result: markdown pre-selected in the in-page textarea, the injected <script> content rendered as literal characters with no alert firing, direct visual confirmation textarea.value was used rather than innerHTML. The real window.open() path itself couldn't be visually confirmed since it never opened - the code writing into it uses the same proven-safe pattern, flagged as inference rather than direct observation. cuesAsText() confirmed byte-identical before and after. Reload-persistence confirmed: identical markdown output from cues reloaded out of localStorage. Not verified: focus recovery after the window trip, since no real window ever opened to trip focus away from the panel. No tags added, per Out Of Scope.
2026-08-08 | brief-021 | ready -> in-progress | Streamer mode versus camera control, closes #16 - blocked, not complete. Step 3 done: the swagger spec has no "streamer" field anywhere, and neither does a live /replay/render read - confirmed the API does not model this setting at all, which per the brief's own Trap means a client-constraint outcome would be documentation-only by necessity (no field exists to build a detection chip against). Step 2's baseline half captured (render state with streamer mode presumably off, no lock active). Step 1 cannot run from this session - it requires physically toggling streamer mode inside the League client's own settings, which is not reachable through the panel, the proxy, or anything else available here. Stopped per the brief's own Escalate instruction ("if step 1 does not reproduce the symptom, stop and ask") rather than guessing which of the three outcomes applies.
2026-08-08 | triage | - -> 10 issues filed | Third triage pass, and the first fed by a screenshot rather than the issue tracker: Fletcher annotated the panel with ten numbered complaints. Filed all ten as issues #18-#27 first so the briefs have something durable to close - the screenshot is not a record. Every one passed the scope test out loud: eight are the panel lying or getting in the way during a take (status chrome, a chip that claims playback with no replay, a copy button that leaves a box on screen, a destructive click with no guard, a transport row you have to read rather than glance at), and #24 is the panel not collecting the data a take is narrated from. Nothing was cut this pass. Grouped into five briefs by UI region rather than by issue, because four of the ten land in the status bar and two more land in the transport row - the collision cost of one-brief-per-issue would have been paid at build time.
2026-08-08 | brief-022 | - -> ready | Status bar chrome, closes #18, #19, #20. Three complaints about one 65px row, so one brief: drop `.card` from the status bar (keeping the rule - four other blocks use it), fuse Cinematic and Setup into a split control, restack Reset under the DISTANCE label. The decision that matters is what is *not* being flattened: brief 015's card stack is right for the roster, transport and cues, which are content; only the status bar is chrome, and only it gets demoted. Found the trap that will otherwise eat the session: `setState()` toggles `.disabled` on `<span id="scene">`, and that span is the only thing stopping camera and HUD writes with no replay loaded - a split control built one level too high silently re-enables them. Second trap: `.setup-toggle` styles three unrelated buttons, so restyling it to make the split control look right also restyles Reset and Hide names.
2026-08-08 | brief-023 | - -> ready | Chips stop claiming things that aren't true, closes #21, #25. Deliberately split from 022 even though it edits the same row: 022 is chrome, this is state correctness, and they touch different children. #25 reproduced during triage with no League client running at all - `clearTransportDisplay()` resets `lastPolled` to `paused: false` and `renderTransport()` maps that to "Playing", so the no-data default and real playback are the same value. The real work is #21, and it is not the chip: `eventsCacheKey()`, `identityKnown()` and `cuesAsMarkdown()` all read the game mode out of the DOM node, so deleting the element without moving the string into a variable silently breaks the event cache and cue persistence. Wrote the byte-for-byte warning in too - `.chip.mode`'s uppercase is presentational, and "fixing" the variable to match what was on screen would invalidate every cache key on disk.
2026-08-08 | brief-024 | - -> ready | Transport row, closes #27 and #26. #27 reverses half of brief 015's grouping and says so: 015 moved seekFwd back beside seekBack for a real reason (it had been exiled to the event steppers), and flanking Pause satisfies that finding as well as the new one. #26 did not reproduce - measured the live panel headless from 820px to 1400px in 20px steps and got zero overlapping pairs at every width, 21px of clearance at 1400px, and a clean wrap below ~1080px. Wrote the measurement into the issue and the brief rather than quietly designing a fix for an unconfirmed mechanism, and made the acceptance test a width sweep instead of a screenshot. What ships regardless is hardening: the five speed buttons are a rigid 412px block that cannot shrink, so shrink is the only mechanism in this row that could overlap, and `flex: 0 0 auto` plus `white-space: nowrap` forces overflow to resolve as a wrap. The brief is instructed not to close #26 if step 6 cannot reproduce it - ask Fletcher for width, zoom and display scaling instead.
2026-08-08 | brief-025 | - -> ready | Cue actions, closes #22, #23. `Copy as text` puts up a textarea with no dismiss path anywhere in the file and only then tries the clipboard; the design note explains the intent honestly, but the guaranteed fallback became the thing you interact with every time. Clipboard-first, and the textarea stays in the file because brief 020's `Export notes` popup-blocked path depends on it - deleting it is the obvious cleanup and it would break the one export path 020 actually verified live. Chose a two-click arm over `window.confirm()` for Clear all on this project's own evidence, not on taste: brief 011 shipped a focus chip because dead keypresses after focus loss confused people, and brief 020's Outcome recorded `window.open()` costing hotkeys and kept a second export path to avoid it. A dialog mid-take costs the take. Cue tags stay barred - brief 013's fence, restated for the third time.
2026-08-08 | brief-026 | - -> ready | Scan replay actually scans, closes #24, unblocks #7. Structured measurement-first like briefs 008 and 021, and step 1 can end the brief: brief 016 rewrote this same function eight commits ago and reported cached-equals-live twice, but both sides of that comparison come from the same feed through the same `mergeEvents()` - agreeing with itself is not completeness, which is brief 010's own rule. So the brief measures the current seek-to-end harvest against a manual full play-through on the same replay and stops if they match. Rejected chunked seeking explicitly and with the reason: brief 017 established the client re-emits events with a new EventID and ~1s of EventTime jitter on every re-pass, and its 2s dedupe bucket still misses 3 of 5 known pairs, so 70 little seeks would maximise exactly the problem that is only half fixed - one forward pass minimises it. The speed ceiling is measured rather than assumed (PASSOFF fact 1: the API returns 200 for values it ignores, so a client reporting 16x while advancing at 4x is the failure to catch). Two traps worth the reading time: `checkLoop()` seeks back to A whenever playback passes B, which turns a play-through scan into an infinite loop if a loop is armed; and `eventsCacheKey()` has no notion of how events were collected, so without a version bump every already-scanned replay serves its old incomplete array and the fix looks like a no-op on the exact replays used to test it. Queued last of the five - it is the only one that needs a live replay and a multi-minute verification, and putting it at the head of the queue would stall the queue the way 021 already has.
2026-08-08 | brief-022 | ready -> complete | Shipped as decided. Two things measured, not assumed: the two-row Distance/Reset column initially made the row 62px against a 52px pre-brief baseline (measured by diffing a stateless copy of the pre-brief file, since step 1's baseline wasn't recorded before editing) - fixed by tightening the column, not the row padding, back to 52px exactly. And `min-width: 84px` on the split control's left half wasn't enough for "Restore HUD" (~92px natural width) - min-width is a floor not a ceiling - raised to 96px, confirmed zero-reflow both directions by DOM rect diff. Verified live by DOM measurement throughout (Browser pane still can't composite frames for a screenshot). Moved to archive.
2026-08-08 | brief-023 | ready -> complete | Shipped as decided. #25 reproduced exactly as written before touching code. The brief expected three separate DOM reads to repoint; found `eventsCacheKey()` and `identityKnown()` already share one `replayIdentity()` helper, so it was three call-sites (`replayIdentity()`, `identityKnown()`, `cuesAsMarkdown()`) fed by one new `gameMode` variable, not three independent fixes. Verified live: event cache and cue persistence both round-trip a reload under the unchanged key shape (`CLASSIC:1623`), export header still prints the real mode, pause/speed unaffected when connected, disconnected chips recover within one poll tick with no stuck label cache. Escalation honestly left open per the brief's own ask: only one replay (mode `CLASSIC`) was available this session, so #21's "the mode never varies" premise is inherited, not independently re-tested.
2026-08-08 | brief-024 | ready -> complete | #27 shipped as decided: seek buttons flank Pause as one visually joined unit (independent borders + squared inner corners + -1px collapse, not one shared box, so Pause stays visibly taller), `#transportCmds` split into `#seekBackGroup`/`#seekFwdGroup` still built through `commandButton()`. Row hardened (`flex: 0 0 auto` on every direct child, `white-space: nowrap` on `.cmd-btn`) per decision. Measured before and after with an iframe sweep, 820-1500px: zero overlaps both times, and hardening measurably tightened the wrap behaviour (row now caps at 3 lines instead of 4, 3-to-2 transition moved earlier). Step 6 run rather than skipped: approximated browser zoom via CSS `zoom` at 0.8-1.5x crossed with 1280/1440/1920px, 12 combinations, zero overlaps in any of them. Per the brief's own escalation rule, **#26 is not closed** - two sessions have now failed to reproduce it under a fairly wide matrix; still need Fletcher's window width, browser zoom and Windows display scaling.
2026-08-08 | brief-025 | ready -> complete | Shipped as decided. Both symptoms reproduced first. Real tooling wrinkle recorded rather than papered over: this session's synthetic `element.click()` doesn't carry transient user activation, so it initially looked like every clipboard copy was failing - switched to the Browser pane's real dispatched click (`computer` tool) plus a clipboard-log shim to confirm the write actually succeeds with the exact `cuesAsText()` output. Clear-all's arm/disarm tested with precise in-page timing (0ms/1500ms/3500ms) since round-trip latency made "immediately after click" unobservable any other way: armed at 0ms, still armed at 1500ms, auto-disarmed by 3500ms, cues untouched throughout. Placing a cue while armed correctly disarms via `renderCueList()`'s new `disarmClear()` call; two clicks 300ms apart clears for real and persists `[]` to localStorage. Escape now cancels the arm instead of also firing the pre-existing `Escape -> loopClear` binding - not spelled out in the brief's decision but consistent with the label/notes "one keypress, one action" precedent. One escalation left honestly open: couldn't screenshot the armed red button to judge "reads as protective, not broken" by eye (Browser pane still can't composite frames) - judged from DOM state and the `--bad` convention instead.
2026-08-08 | brief-026 | ready -> complete | Step 1 measurement gate did not agree - old seek-to-end found 92 events, a full manual play-through found 101 (missing 5 ChampionKill, 2 TurretKilled, 1 Multikill, 1 Ace) - so the full rewrite ran. Speed probe's first attempt gave a false "everything failed" reading because the replay was parked at time≈length with no runway; re-run properly, 4/8/16/32x all held, shipped 32x (~51s for a 27min replay). Avoided calling requestSeek()/setSpeed()/togglePause() from inside the scan's own withMutex() sequences (nested withMutex deadlocks; a relative pause-toggle would race lastPolled's stale pre-seek value) - wrote the start and restore sequences directly against postPlayback()/waitForSeekSettled(). Verified live: exact restoration of time/speed/paused/loop-armed-state after a completed scan and after a cancel, progress genuinely tracks polled time, cache key versioned (v2) so old incomplete arrays don't serve silently, cue cursor explicitly cleared on scan start (step 7's decision, stated). Honest finding, not swept under the rug: repeated scans against the same still-open replay client produced a growing count (101->106->109) - checked and it's brief 017's already-documented jitter-duplicate problem (34 same-EventName pairs within 2s in the 106-set), not something new, but a full pass is longer exposure to it than a two-second settle-poll was, and a clean before/after pair-count wasn't possible since the old method's raw list wasn't preserved. #7 evidence handed over, not decided: zero DragonKill/BaronKill/HeraldKill across every harvest this session including the now-provably-complete ones.
2026-08-08 | - | triage | Fourth triage pass, run against the 5 issues left undispositioned after the screenshot pass (#28-#32) plus the residue the last session's own Outcomes recorded. Every open issue was re-read including the wontfix ones, and every one had its code opened before a disposition was written - which is what turned three of them from design discussions into mechanisms. Four briefs (027-030), queue is 027 -> 028 -> 029 -> 030 after the still-blocked 021. Scope test cleared all four out loud: 027 is the panel telling you a fight had eight kills when it had five, 029 is the hover card refusing to open on the part of the marker your eye goes to, 030 is a 60px scrub track, 028 is the one glance-not-read control in the status bar pointing the wrong way. Nothing cut. Three findings the issues did not carry. (1) #28 is arithmetic, not rendering: nearestCluster() measures Math.abs(pxX - px) against SNAP_PX=8 while a clustered marker is up to 26px wide and anchored by its LEFT edge, so the middle and right of every wide cluster is outside the magnet and the container's own mousemove handler schedules the hide - the count digit sits at pxX+13, dead centre of the dead band. Clicking it works, because that is a direct listener on the element; hover and click disagree about where the marker is. (2) The screenshots on #29 decompose into three separate mechanisms, not one scaling problem - a max-width the panel outgrew, a .timeline-row that never wraps against a flex-shrink:0 330px rail (which is also why the legend smears across the event list: .legend has no flex-wrap and .scrub-area is min-width:0), and clustered markers growing rightward past the track's right edge onto the rail, which is what those 85/16 badges are doing over the Events header. (3) #13 is not finished and brief 026 made it matter more: the 2s dedupe bucket is a fixed grid (Math.round(t*0.5), boundaries on odd seconds), so its effective tolerance is anywhere from 0s to 2s depending where a pair lands - brief 017 measured 3 surviving pairs at 0.91/0.25/0.18s and brief 026 measured 101->106->109 across repeated scans. That became brief 027 and it goes first, because it changes the cluster counts 029 and 030 both measure against. Dispositions for the rest: #31 (OBS LAN stream in-browser) has an empty body and is a multi-session architectural feature - not briefed, needs a written premise from Fletcher before the scope test can even be applied honestly; #32 stays a constraint record per Fletcher's explicit "keep it on GitHub and we'll move on", still wontfix, possibly already fixed by the uncommitted CSS Grid rewrite but unconfirmed without his Firefox; #26 stays blocked on his width/zoom/display-scaling numbers and is deliberately kept OUT of brief 030 so that brief does not inherit an acceptance test no session can run; #16 stays blocked behind brief 021's physical client toggle; #7 stays open with brief 026's evidence attached and undecided; #14's residue (the empty objective lane, and EVENT_CATEGORY still having no void-grub entry) is the same question as #7 and stays with it rather than becoming its own brief, since the no-invented-strings rule means it cannot be written until a replay with objectives exists. Collision noted and written into both briefs: 029 and 030 both own the marker gutter - 029 owns where the hit test believes a marker is, 030 owns where the marker's box actually is - so 030 re-runs 029's sweep as a step rather than assuming it.
2026-08-08 | brief-027 | - -> ready | Dedupe by gap from an anchor instead of by a fixed grid, finishes #13. The mechanism is the one brief 017 reported and did not chase: Math.round(event.EventTime * 0.5) is a grid whose cells break on odd seconds, so a 0.18s pair straddling a boundary survives while a 1.9s pair inside one merges - the tolerance is nominally 2s and effectively unknowable. Decision is a time-free eventIdentity() plus a per-identity anchor list with a real gap test, keeping eventsByKey a Map of events so visibleEvents() and the cache write need no changes. The anchor deliberately never moves: without that, copies at t, t+1.9, t+3.7 chain into one merged event spanning 3.7s, which is the false merge the 2s ceiling exists to stop. First-copy-wins replaces the unconditional set, which also stops marker positions drifting between poll ticks. Rejected a wider bucket (moves the seams, does not remove them), a grid-plus-neighbour-check (a gap test written the long way), and render-time dedupe (leaves the duplicates in the cache and in every .size). Departs from brief 026's version-bump precedent on purpose and says so: loadCachedEvents() already re-merges cached arrays, so old v2 caches self-heal on load and a bump would instead force a ~51-second re-scan of every replay Fletcher has already scanned - step 5 proves it rather than assuming it. Queued first because it changes the cluster counts briefs 029 and 030 measure.
2026-08-08 | brief-028 | - -> ready | The setup caret points where it goes and sits centred, closes #30. Both halves of the issue have the same cause and it is the one that has already cost this project a session: the caret is the literal character U+2304 in the markup, so its ink sits wherever the fallback font puts it - the same font-fallback family as #32's glyphs, which measured the transport row at 66px in Firefox against ~45px in Chromium. line-height:1 fixed a row height; it cannot centre a glyph that is off-centre inside its own box. Decision is a CSS border chevron on a pseudo-element, rotate(45deg) closed and rotate(135deg) open, because a drawn chevron has no font to fall back to and its geometry is arithmetic - which makes "centred" checkable by rect comparison in a session that still cannot composite a screenshot. Rejected swapping the character on toggle (two fallback glyphs cannot both be centred by one rule), inline SVG (works, but needs colour plumbing for .open and buys nothing extra), and a top:-1px nudge (a magic number tuned against one browser's fallback font, in the file that has been bitten by exactly that twice). Found in passing: .control-split-caret.open sets border-color: var(--accent) on an element with border: none - dead since brief 022 wrote it, and it would come back to life pointing at the wrong thing the moment a border-drawn chevron lands, so the brief deletes it before adding borders. The existing handler already derives .open correctly (it reads hidden before flipping it), so no new state.
2026-08-08 | brief-029 | - -> ready | The hover magnet measures a point, so wide clusters have a dead zone, closes #28. Triage turned this from "the popup is flaky" into a number: nearestCluster() compares Math.abs(candidate.pxX - px) against SNAP_PX=8, pxX is a clustered marker's LEFT edge by brief 014's deliberate design (the left edge is the first event's real time), and clusterWidthPx() makes a six-plus cluster 26px wide - so pxX+8 to pxX+26 is inside the marker and outside the magnet, and the gutter mousemove handler calls scheduleHideHoverCard() there on every pixel. The count digit is centred at pxX+13. marker.mouseenter fires once and cannot cancel a hide scheduled 140ms later, and .marker::before's 16px hit target is narrower than the marker it is padding. Single markers are unaffected because they are translateX(-50%) and under 7px, which is exactly why the issue names the number. Decision is interval distance - the marker's rendered span recorded by buildMarker() from the same values it writes into style.left/style.width, never read back with getBoundingClientRect() (single kill markers are rotated 45deg and every marker gains scaleY(1.35) on hover, so a measured rect answers a different question depending on when you ask). Both callers get it including click-snap, because a click on a wide cluster's right half currently misses it too - the same defect wearing different clothes. SNAP_PX explicitly does not change value; the comment at its declaration records that 8 was measured against ~15px marker packing and raising it was already rejected once.
2026-08-08 | brief-030 | - -> ready | The panel stops wasting a wide screen and stops crushing a narrow one, closes #29. Three screenshots, three mechanisms, and the brief refuses to treat them as one scaling problem. Wide: .panel's max-width is 1400px and the comment above it is the argument against its own value - it is the reasoning that took the panel from 720 to 1400 and then stopped, one screen size ago. Narrow: .timeline-row has no wrap and .rail is width:330px/flex-shrink:0 against .scrub-area's min-width:0, so the rail always wins and the track pays - ~60px of track at 492px - and .legend has no flex-wrap either, which is the smeared text over the event rows rather than a z-index bug. Zoomed: the same narrow path plus clustered markers growing rightward past the track's right edge onto the rail, which is what the 85 and 16 badges are doing over the Events header. Decisions: cap to 1800px stated honestly as a cap (extreme zoom-out will always leave margin; the point is that 1400 stops binding at 1920 and 2560), a container query that stacks the rail under the track below a measured threshold, a real min-width floor on .scrub-area, flex-wrap on .legend, and markers clamped inside the track rather than clipped - brief 018 spent a whole brief getting a readout OUT of a clipping context and a sliced count digit is worse than a displaced marker. Written to avoid flex:1 0 0 on either side of that row deliberately: the file already records Mozilla #1179454 biting exactly that shape in Fletcher's Firefox last session, and .rail/.scrub-area are that shape. #26 is kept out with the reason stated - folding it in would hand this brief an acceptance test no session can run. Queued last: it is the widest blast radius and it re-runs brief 029's hover sweep as a step, since the two briefs share the marker gutter.
2026-08-08 | brief-031 | - -> ready | Drive the panel from the other machine, tests the control half of #31. Fletcher's sequencing call: the video card goes on the backlog until normal LAN control is tested and wrapped. Written as an investigation brief in briefs 008/021's shape - it tests, and ships code only if the test finds something - because every fact in its Problem Statement is a code reading rather than an observation, and this project has twice built against an assumed mechanism. The reading says it already works with no code at all: server.listen(PORT) passes no host so Node binds every interface, the client builds its socket as ws://${location.host}, and there is no absolute URL anywhere in the file. Two origin differences found by reading and written in rather than discovered mid-build: navigator.clipboard does not exist on a non-secure origin, and http://<lan-ip>:3000 is not one - exportCues() catches the resulting TypeError inside its existing try, so brief 025's clipboard-first fix degrades silently to the textarea, which is exactly the behaviour #22 was filed about; and localStorage is per-origin, so the LAN machine starts with no cues, no event cache, no follow distance, no hideNames and no cinematicRestore. Both are reported, not fixed - which fix is right depends on whether the LAN machine becomes the panel's normal home, and that is Fletcher's answer to give. The real prize is step 5: hotkeys while the League client holds focus, which has never worked and is the wall brief 007 was written around and brief 011 shipped a focus chip for - two machines means two focus contexts. Explicitly no HTTPS, no certificates, no WebRTC: RTCPeerConnection needs a secure context and that is the video half's prerequisite, which is the part that was just deferred. Runs FIRST, ahead of 027-030, and the broken numeric order is deliberate and recorded - it is cheap, it gates #31, and TRIAGE's own rule is that renumbering four already-pushed briefs costs more than documenting one exception.
2026-08-08 | - | process | Three standing instructions from Fletcher written into the docs rather than left in a chat log. (1) GitHub is the source of truth and nothing stays in the working tree between sessions - wraps, Stage 4 brief closes and triage passes all commit and push without being asked, ending with git status --short and git log origin/main..main both empty. This reverses TRIAGE.md's old hard rules (no pushing, no issue comments, no closing issues without permission), which produced a day where five shipped briefs, a UI overhaul and a whole triage pass sat uncommitted in one tree and the next pass's briefs had to anchor their line numbers to "commit X plus uncommitted changes". Closing shipped issues and posting decision comments are granted too; changing an issue's meaning (reopen, relabel, wontfix) still gets asked. The old rules are kept in TRIAGE.md as a named reversal rather than deleted, because the reasoning that produced them is worth seeing next to the reasoning that overturned them. (2) Chromium is the build and verification target - Fletcher was on Firefox but can run Brave or Opera, and the Firefox-only detour already cost most of a session; broader browser support stays a real goal, later. PASSOFF carries the standing preference for Grid, container queries and drawn glyphs over nested flex sizing and font characters, which is better CSS regardless of engine. (3) Found while acting on (1): .gitignore's `.claude/*` was swallowing .claude/skills/, so the triage-briefs and wrap-session entry points had never been tracked - the 2026-08-07 log entry adding triage-briefs says it exists "so the process is invocable rather than remembered" and a fresh clone remembered nothing. Now tracked; settings.local.json and transcripts stay ignored. That one was the process instruction failing in the same breath it was being written down.
